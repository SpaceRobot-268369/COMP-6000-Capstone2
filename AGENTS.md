# Agent Instructions

This repository uses `CLAUDE.md` as the canonical agent guidance file.

Before making changes, read `CLAUDE.md` and follow its instructions for project
context, coding standards, git workflow, branch naming, DVC usage, testing, and
documentation.

Do not duplicate long-lived agent rules here. Update `CLAUDE.md` instead.
